{
  "name": "toho-html-data",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "generate-manifests": "bun run generate-iiif-manifests.ts",
    "update-index": "bun run update-index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {},
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
